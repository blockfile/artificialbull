{
  "name": "artificialbull-api",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only stats API for the Artificial Bull site — market cap, holders, and what pons's fee distributor has paid holders. Distributes nothing itself.",
  "homepage": "https://artificialbull.com",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node scripts/check.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
